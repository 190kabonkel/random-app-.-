fn main() {
    println!("Hello, world!");
    println!("vinci software wants to expand into rust");
    println!("cargo build to compile into a executable rust project this wil be verry slow")
    println!("cargo run wil run an exacutable rust project")
    println!("cargo build --version wil show the version of a project")
    println!("rustc main.rs  is to comile and rust file called main into an exe")
}
